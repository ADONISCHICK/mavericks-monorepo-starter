{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "HOST=0.0.0.0 next start -p ${PORT:-3000}",
    "lint": "eslint"
  },
  "dependencies": {
    "next": "15.5.0",
    "react": "19.1.0",
    "react-dom": "19.1.0"
    /* keep your other deps here exactly as you have them */
  },
  "devDependencies": {
    /* keep your dev deps here exactly as you have them */
  }
}
